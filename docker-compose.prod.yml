services:
  app:
    build:
      context: .
      target: production
    container_name: acquisition-api
    env_file:
      - .env.production
    environment:
      NODE_ENV: production
    ports:
      - "8080:8080"
